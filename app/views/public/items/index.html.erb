<h3>商品一覧</h3>
<% @items.each do |item| %>
<div>
   <%= link_to(item_path(item)) do %>
     <%= attachment_image_tag item, :image %>
     <%= item.name %>
     <%= item.price %>
   <% end %>
</div>
<% end %>